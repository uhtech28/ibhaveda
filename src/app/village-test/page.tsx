import { VillageTestClient } from "./VillageTestClient";

export const metadata = {
  title: "Village Test Map · Ibhaveda",
  description: "LDtk painted village — WASD to walk, click checkpoints.",
};

export default function VillageTestPage() {
  return <VillageTestClient />;
}

"use client";
// STUB — @excalidraw/excalidraw was referenced but never installed.
// Original file replaced to unblock production build. Restore the real
// component after `bun add @excalidraw/excalidraw` and a schema pass.
export default function ExcalidrawTool() {
  return (
    <div className="p-6 text-sm text-white/60">
      Excalidraw tool temporarily unavailable — pending dependency install.
    </div>
  );
}
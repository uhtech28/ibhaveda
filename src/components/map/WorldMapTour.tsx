"use client";

/**
 * REMOVED - replaced by the v2 tutorial (Sparky) at
 * @/components/tutorial/v2. This file is kept as a stub so any residual
 * imports across the codebase don't crash the build; it renders nothing.
 */

export function WorldMapTour(_props: {
  show?: boolean;
  onClose?: () => void;
  ventureName?: string;
}) {
  return null;
}
